<div class="media">
   <div class="media-body">
     <h4 class="media-heading">
       <%= link_to item.title, item_path(item, item) %>
     </h4>
     <small>
       submitted <%= time_ago_in_words(post.created_at) %> ago by <%= item.user.name %> <br>
     </small>
   </div>
 </div>
